import React from 'react'
import { StyledSelect } from './style'

const ControlledSelect = (props): JSX.Element => {
  return (
      <StyledSelect
        {...props}
      />
  )
}

export default ControlledSelect
